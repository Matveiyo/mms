# Agents

## Cursor Cloud specific instructions

This repository (`mms`) is currently empty — it contains only a placeholder `README.md`. There are no applications, services, dependencies, build tools, test frameworks, or linting tools configured yet.

### Current state
- **No source code or application** exists to build or run.
- **No package manager lockfile** (`package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `requirements.txt`, etc.) is present.
- **No test framework** is configured.
- **No linting or formatting** tools are set up.
- **No Docker or container** configuration exists.

### When code is added
Once application code is added to this repository, update this file with:
- How to install dependencies
- How to run the application in development mode
- How to run tests and linting
- Any non-obvious caveats or gotchas for the dev environment
